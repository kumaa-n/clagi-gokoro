<div data-controller="auto-modal">
  <%= yield %>
  <dialog class="modal" data-auto-modal-target="dialog">
    <div class="modal-box bg-white rounded-2xl shadow-xl border border-base-200/60 p-6 md:p-8">
      <h3 class="font-serif font-medium text-xl text-primary mb-4">レビューの削除</h3>
      <p class="py-4 text-neutral/80">レビューを削除すると元に戻せません。<br>本当に削除してもよろしいですか？</p>
      <div class="modal-action flex gap-3">
        <form method="dialog" class="flex-1">
          <button class="btn btn-ghost text-neutral/60 w-full hover:bg-neutral/5 rounded-full">キャンセル</button>
        </form>
        <%= button_to "削除する",
          review_path(review),
          method: :delete,
          class: "btn btn-error text-base-100 w-full shadow-md hover:shadow-lg transition-all hover:-translate-y-0.5 rounded-full flex-1",
          form: { class: "flex-1", data: { action: "submit->auto-modal#hide" } } %>
      </div>
    </div>
    <form method="dialog" class="modal-backdrop bg-neutral/20 backdrop-blur-sm">
      <button>close</button>
    </form>
  </dialog>
</div>
