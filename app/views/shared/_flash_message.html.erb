<% flash.each do |message_type, message| %>
  <% next if message_type.to_sym == :review_prompt_song_id %>
  <div class="toast toast-top toast-center z-10" data-controller="flash">
    <div class="alert <%= flash_class(message_type) %> mx-5 my-5">
      <%= message %>
    </div>
  </div>
<% end %>
