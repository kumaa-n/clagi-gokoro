<div data-controller="auto-modal">
  <%= yield %>
  <dialog class="modal" data-auto-modal-target="dialog">
    <div class="modal-box bg-white rounded-2xl shadow-xl border border-base-200/60 p-6 md:p-8">
      <h3 class="font-serif font-medium text-xl text-primary mb-4">アカウント削除の確認</h3>
      <p class="py-4 text-neutral/80">本当にアカウントを削除しますか？</p>
      <div class="p-4 rounded-xl border border-base-200">
        <p class="font-medium text-neutral/90 mb-2">以下のデータが削除されます：</p>
        <ul class="list-disc list-inside text-sm text-neutral/80 space-y-1 ml-2">
          <li>あなたが投稿したすべてのレビュー</li>
          <li>お気に入りに登録したレビュー</li>
          <li>プロフィール情報</li>
        </ul>
        <p class="mt-3 text-sm text-error font-medium">※ この操作は取り消せません</p>
      </div>
      <div class="modal-action flex gap-3 mt-6">
        <form method="dialog" class="flex-1">
          <button class="btn btn-outline w-full rounded-full px-8">キャンセル</button>
        </form>
        <%= button_to "削除する",
                      users_path,
                      method: :delete,
                      class: "btn btn-error text-base-100 w-full shadow-md hover:shadow-lg transition-all hover:-translate-y-0.5 rounded-full flex-1",
                      form: { class: "flex-1", data: { action: "submit->auto-modal#hide", turbo: false } } %>
      </div>
    </div>
    <form method="dialog" class="modal-backdrop bg-neutral/20 backdrop-blur-sm">
      <button>close</button>
    </form>
  </dialog>
</div>
