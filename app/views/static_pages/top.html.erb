<div class="relative h-64 md:h-80 lg:h-96">
  <%= image_tag "hero-guitar.png", class: "absolute inset-0 w-full h-full" %>
  <div class="absolute inset-0 bg-black/20"></div>
  <div class="relative flex items-center justify-center h-full text-base-100 px-4">
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-light text-center">
      クラシックギターの難易度をシェアしてみよう
    </h2>
  </div>
</div>

<section class="py-8 md:py-12 lg:py-16 bg-base-200 px-4">
  <h3 class="text-2xl md:text-3xl text-center mb-8 md:mb-12 font-bold text-primary">クラギごころとは？</h3>

  <div class="text-primary text-center max-w-4xl mx-auto">
    <p class="mb-3 md:mb-2">クラギごころはクラシックギターの曲を「テンポ」「運指技巧」「弾弦技巧」「表現力」「暗譜・構成理解」の5つの項目で評価し、レーダーチャートとして可視化できるWebサービスです。</p>
    <p class="mb-3 md:mb-2">「この曲、自分にも弾けるかな？」という悩みを、他の演奏者の評価と比較することで解決します。</p>
    <p class="mb-3 md:mb-2">まだ知られていない曲との出会いや、自分に合ったレパートリーの発見をサポートします。</p>
  </div>
</section>

<section class="pt-8 md:pt-12 lg:pt-16 pb-6 md:pb-8 lg:pb-10 bg-base-100">
  <div class="container mx-auto px-4">
    <h3 class="text-2xl md:text-3xl mb-8 md:mb-12 font-bold text-primary">レビューの多い曲</h3>

    <% if @most_reviewed_songs.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 px-4 md:px-0">
        <% @most_reviewed_songs.each do |song| %>
          <%= render "shared/song_card", song: song %>
        <% end %>
      </div>
    <% else %>
      <p class="text-center text-neutral">まだレビュー付きの曲がありません。</p>
    <% end %>
  </div>
</section>

<section class="py-6 md:py-8 lg:py-10 bg-base-100">
  <div class="container mx-auto px-4">
    <h3 class="text-2xl md:text-3xl mb-8 md:mb-12 font-bold text-primary">最近追加された曲</h3>

    <% if @recent_songs.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 px-4 md:px-0">
        <% @recent_songs.each do |song| %>
          <%= render "shared/song_card", song: song %>
        <% end %>
      </div>
    <% else %>
      <p class="text-center text-neutral">まだ曲が投稿されていません。</p>
    <% end %>
  </div>
</section>

<section class="pt-6 md:pt-8 lg:pt-10 pb-8 md:pb-12 lg:pb-16 bg-base-100 px-4">
  <div class="container mx-auto text-center border border-base-300 rounded-lg p-6 md:p-10">
    <h3 class="text-2xl md:text-3xl mb-6 md:mb-8 font-bold text-primary">
      難易度をシェアし、自分に合った曲を見つけよう
    </h3>

    <div class="max-w-4xl mx-auto mb-6 md:mb-8 text-primary text-center">
      <p class="mb-3 md:mb-2">あなたが感じた曲の難しさを星評価で投稿し、レーダーチャートで可視化できます。</p>
      <p class="mb-3 md:mb-2">「テンポは速いけど、運指はシンプル」「表現力は求められるけど、暗譜は簡単」といった、曲の特徴を多角的に把握することで、自分に合った次の一曲が見つかります。</p>
      <p class="mb-3 md:mb-2">一人で練習することが多いクラシックギターだからこそ、感じ方をシェアし、演奏者それぞれの視点を知りましょう。</p>
    </div>

    <% if user_signed_in? %>
      <%= link_to "曲一覧へ", songs_path, class: "btn md:btn-lg btn-primary text-base-100 px-8 md:px-12 w-full md:w-auto" %>
    <% else %>
      <%= link_to "新規登録する", new_user_registration_path, class: "btn md:btn-lg btn-primary text-base-100 px-8 md:px-12 w-full md:w-auto" %>
    <% end %>
  </div>
</section>
